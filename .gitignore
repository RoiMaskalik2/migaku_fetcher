# Migaku raw dumps — large and contain personal account data
migaku_data/pull_sync.json
migaku_data/srs.db

# Text + tokenisation caches — large, regenerated automatically
data/epub_text_*.txt
data/srt_text_*.txt
data/word_counts_*.json
srt_text_*.txt

# Default-named pipeline output (use named outputs instead)
srt_vocab.html

# Python
__pycache__/
*.pyc
*.pyo
